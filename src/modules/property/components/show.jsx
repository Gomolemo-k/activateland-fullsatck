// import ShowFormProperty from "./show.form.jsx";

function ShowProperty(currentUser) {
    return (
        <div></div>
        // <ShowFormProperty disabled={true} currentUser={currentUser} />
    );
}

export default ShowProperty;