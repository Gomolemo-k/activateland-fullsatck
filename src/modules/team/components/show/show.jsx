// import ShowFormTeam from "./show.form.jsx";

function ShowTeam(currentUser) {
    return (
        <div></div>
        // <ShowFormTeam disabled={true} currentUser={currentUser} />
    );
}

export default ShowTeam;