// import ShowFormWorkspace from "./show.form.jsx";

function ShowWorkspace(currentUser) {
    return (
        <div></div>
        // <ShowFormWorkspace disabled={true} currentUser={currentUser} />
    );
}

export default ShowWorkspace;