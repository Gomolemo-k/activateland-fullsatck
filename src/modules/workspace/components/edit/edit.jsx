// import EditFormWorkspace from "./edit.form.jsx";

function EditWorkspace(currentUser) {
    return (
        <div></div>
        // <EditFormWorkspace disabled={false} currentUser={currentUser} />
    );
}

export default EditWorkspace;