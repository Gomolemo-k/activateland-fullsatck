// import ShowFormPropertyAnalysis from "./show.form.jsx";

function ShowPropertyAnalysis(currentUser) {
    return (
        <div></div>
        // <ShowFormPropertyAnalysis disabled={true} currentUser={currentUser} />
    );
}

export default ShowPropertyAnalysis;